const author : string = 'Abati Babatunde'

var message : string = `Welcome ${author}`;

function log(message) {
    console.log(message);
}
log(message);


const reader: string = 'Abati Daniel'

var newMessage = `Welcome ${reader}`;